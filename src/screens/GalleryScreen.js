import React from 'react'
import data from '../data/data.js'

const GalleryScreen = () => {
    const {photos} = data
    return (
        <div>
            
        </div>
    )
}

export default GalleryScreen
